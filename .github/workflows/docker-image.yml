name: Docker Image CI

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:

  build:

    runs-on: mayur

    steps:
    - uses: actions/checkout@v3
    - name: Build the Docker image
      run: docker images | docker build .  --tag sample
